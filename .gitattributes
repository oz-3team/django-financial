# Set default behavior to automatically normalize line endings to LF
* text=auto eol=lf

# Explicitly set LF for common text file types
*.py text eol=lf
*.html text eol=lf
*.css text eol=lf
*.js text eol=lf
*.json text eol=lf
*.md text eol=lf
*.txt text eol=lf
*.yaml text eol=lf
*.yml text eol=lf
*.lock text eol=lf

# Windows 환경에서 기존 파일 LF로 변환
# git rm --cached -r .
# git reset --hard

# or

# git add --renormalize .
# git commit -m "Normalize line endings to LF"